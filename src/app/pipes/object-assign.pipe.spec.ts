import { ObjectAssignPipe } from './object-assign.pipe';

describe('ObjectAssignPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectAssignPipe();
    expect(pipe).toBeTruthy();
  });
});
